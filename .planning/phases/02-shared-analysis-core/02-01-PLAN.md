---
phase: 02-shared-analysis-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analysis_core.py
  - main.py
  - predict_prophet.py
  - tests/test_analysis_core.py
autonomous: true
requirements:
  - ANLY-01
must_haves:
  truths:
    - "The supported analysis math and price-loading path live behind one shared module instead of duplicated functions in `main.py` and `predict_prophet.py`."
    - "The shared loader fails with clear operator-facing errors when `asset_prices` data or required columns are unavailable."
    - "The forecast script reuses shared prep helpers where they fit without making Prophet modeling part of the supported core."
  artifacts:
    - "`analysis_core.py` exports the shared loading, slicing/alignment, cumulative-return, regression-band, and frame-preparation helpers."
    - "`tests/test_analysis_core.py` covers normalized loading, missing-column failures, regression prep, and short-window errors."
    - "`main.py` and `predict_prophet.py` import shared helpers instead of keeping their own full copies of the same prep logic."
  key_links:
    - "`main.py` imports `analysis_core.py` for `load_price_history(...)`, `calculate_cumulative_pct_change(...)`, `calculate_regression_bands(...)`, `get_regression_band(...)`, and `prepare_analysis_frames(...)`."
    - "`predict_prophet.py` imports shared loading/alignment helpers from `analysis_core.py` while keeping `Prophet()` model fitting local."
    - "Any `asset_prices` failure in the shared loader raises or returns the same actionable operator message regardless of caller."
---

<objective>
Create the shared analysis-preparation core so all supported runtime paths can load prices, slice periods, and compute regression-band inputs through one contract.

Purpose: Deliver ANLY-01 and the data-prep half of ANLY-02 by removing the duplicated loading/alignment/math logic currently split between `main.py` and `predict_prophet.py`.
Output: `analysis_core.py`, direct coverage for the shared math/loader contract, and both entrypoints importing the shared helpers.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-shared-analysis-core/02-CONTEXT.md
@.planning/phases/02-shared-analysis-core/02-PATTERNS.md
@.planning/phases/01-configuration-foundation/01-CONTEXT.md
@.planning/codebase/CONCERNS.md
@main.py
@predict_prophet.py
@service_bootstrap.py

<interfaces>
Preserve these supported behaviors while moving them behind one module:

```python
def load_data(symbol, source, start_date=None, end_date=None, data_type="adjusted", data_dir=None, read_symbol_data_fn=None):
    ...

def calculate_cumulative_pct_change(df, start_date, end_date, sma_window=None, plot_bollinger_bands=False):
    ...

def calculate_regression_bands(df, max_stddev=4):
    ...

def get_regression_band(current_pct, df_truncated):
    ...
```

Create this shared interface in `analysis_core.py`:

```python
def load_price_history(symbol, source, start_date=None, end_date=None, data_type="adjusted", data_dir=None, read_symbol_data_fn=None):
    ...

def slice_period(df, start_date, end_date):
    ...

def calculate_cumulative_pct_change(df, start_date, end_date, sma_window=None, plot_bollinger_bands=False):
    ...

def calculate_regression_bands(df, max_stddev=4):
    ...

def get_regression_band(current_pct, df_truncated):
    ...

def prepare_analysis_frames(df, symbol, original_start, original_end, new_start, adjusted_new_end, sma_window, plot_bollinger_bands):
    ...

def prepare_aligned_period(df, start_date, end_date):
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the shared loader, slicing, and regression-band helpers</name>
  <files>analysis_core.py, tests/test_analysis_core.py</files>
  <read_first>AGENTS.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/02-shared-analysis-core/02-CONTEXT.md, .planning/phases/02-shared-analysis-core/02-PATTERNS.md, main.py, predict_prophet.py, service_bootstrap.py</read_first>
  <behavior>
    - `load_price_history(...)` normalizes the reader output to `index` and `adj_close`, preserving the current `main.py` column-handling behavior and operator-facing failure messages.
    - `prepare_analysis_frames(...)` reproduces the current `main.py` regression-band input contract and still raises on empty or too-short reference windows.
    - `prepare_aligned_period(...)` provides the date-sliced aligned DataFrame needed by the Prophet workflow without pulling forecast logic into the shared core.
  </behavior>
  <action>Create `analysis_core.py` as the root-level shared analysis module. Move the concrete logic now in `main.py::load_data`, `calculate_cumulative_pct_change`, `calculate_regression_bands`, `get_regression_band`, and `_prepare_analysis_frames` into this module with the exact function names `load_price_history`, `calculate_cumulative_pct_change`, `calculate_regression_bands`, `get_regression_band`, and `prepare_analysis_frames`. Add a concrete helper named `slice_period(df, start_date, end_date)` that centralizes the current timezone-aware date filtering used in both `main.py` and `predict_prophet.py`, plus `prepare_aligned_period(df, start_date, end_date)` that returns a sliced frame with `day_index`, `daily_pct_change`, and `cumulative_pct_change` for forecast prep. Keep return math and regression-band semantics exactly as they behave today; do not change additive-vs-compounded methodology in this phase. Add `tests/test_analysis_core.py` using stdlib `unittest` and small in-memory DataFrame fixtures to verify normalized loading, missing `adj_close` failures, short reference-window failure messages, regression-band columns, and aligned-period output columns.</action>
  <verify>
    <automated>python3 -m unittest tests.test_analysis_core -v</automated>
  </verify>
  <acceptance_criteria>
    - `analysis_core.py` contains `def load_price_history(`, `def slice_period(`, `def calculate_cumulative_pct_change(`, `def calculate_regression_bands(`, `def get_regression_band(`, `def prepare_analysis_frames(`, and `def prepare_aligned_period(`.
    - `python3 -m unittest tests.test_analysis_core -v` exits `0`.
    - `tests/test_analysis_core.py` proves a missing `adj_close` column raises an actionable error and that a one-row reference window raises the current short-window failure.
  </acceptance_criteria>
  <done>The shared analysis-prep contract exists with direct automated coverage for loading, slicing, regression-band math, and aligned-period preparation.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire the supported and experimental entrypoints onto the shared prep core</name>
  <files>main.py, predict_prophet.py</files>
  <read_first>AGENTS.md, .planning/ROADMAP.md, .planning/phases/02-shared-analysis-core/02-CONTEXT.md, .planning/phases/02-shared-analysis-core/02-PATTERNS.md, analysis_core.py, main.py, predict_prophet.py</read_first>
  <action>Update `main.py` to import the shared helpers from `analysis_core.py` and remove the local definitions of `load_data`, `calculate_cumulative_pct_change`, `calculate_regression_bands`, `get_regression_band`, and `_prepare_analysis_frames`. Replace their call sites with the exact shared names `load_price_history(...)` and `prepare_analysis_frames(...)`. Update `predict_prophet.py` to import `load_price_history(...)` and `prepare_aligned_period(...)` from `analysis_core.py`, remove its local `load_data(...)` implementation, and stop duplicating timezone/date-slice logic now covered by the shared helpers. Keep Prophet model fitting, `prepare_prophet_data(...)`, `predict_for_new_period(...)`, and the forecast-specific `plot_comparison(...)` local to `predict_prophet.py`. Do not move forecast chart rendering into the supported shared core in this plan.</action>
  <verify>
    <automated>python3 -m py_compile analysis_core.py main.py predict_prophet.py &amp;&amp; rg -n 'from analysis_core import' main.py predict_prophet.py &amp;&amp; ! rg -n '^def load_data\\(|^def calculate_cumulative_pct_change\\(|^def calculate_regression_bands\\(|^def get_regression_band\\(' main.py predict_prophet.py</automated>
  </verify>
  <acceptance_criteria>
    - `main.py` imports shared analysis helpers from `analysis_core.py`.
    - `predict_prophet.py` imports shared loading/alignment helpers from `analysis_core.py` and no longer defines its own `load_data(` function.
    - `python3 -m py_compile analysis_core.py main.py predict_prophet.py` exits `0`.
  </acceptance_criteria>
  <done>The monitor and forecast entrypoints now share the same analysis-preparation contract while keeping Prophet-only modeling isolated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| configured `asset_prices` reader -> shared analysis core | External market-data rows cross into the shared normalization and math pipeline |
| shared analysis helpers -> supported commands | Changes in one helper affect `run`, `test-email`, and later `report` behavior together |
| shared prep helpers -> experimental Prophet path | Experimental forecast code consumes common loading/alignment helpers but must not weaken supported-command failure policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | T | `analysis_core.py` input normalization | mitigate | Preserve the exact `index` / `adj_close` normalization rules from `main.py` and fail with actionable messages on schema mismatch |
| T-02-01-02 | D | shared prep helpers | mitigate | Keep short-window and empty-range failures explicit so supported commands hard-fail instead of producing partial analysis |
| T-02-01-03 | E | Prophet reuse boundary | mitigate | Import only shared loading/alignment helpers into `predict_prophet.py`; keep forecast modeling and forecast-only plots local so experimental behavior cannot redefine the supported core |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python3 -m unittest tests.test_analysis_core -v`
- [ ] `python3 -m py_compile analysis_core.py main.py predict_prophet.py`
- [ ] `rg -n 'from analysis_core import' main.py predict_prophet.py`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Shared loading, slicing, and regression-band helpers live in `analysis_core.py`
- `main.py` and `predict_prophet.py` use the same prep helpers where Phase 2 requires convergence
- Supported commands retain clear operator-facing failures for missing data and broken reference windows
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-analysis-core/02-01-SUMMARY.md`
</output>
