---
phase: 02-shared-analysis-core
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - main.py
  - predict_prophet.py
  - README.md
  - tests/test_main_cli.py
autonomous: true
requirements:
  - ANLY-02
  - ANLY-03
must_haves:
  truths:
    - "Supported operator commands execute through the shared analysis/report core instead of keeping stale call paths."
    - "The repo exposes a supported no-email one-shot report command under `main.py`."
    - "The forecast script reuses shared helpers where they fit, while staying explicitly experimental."
  artifacts:
    - "`main.py` includes a supported `report` subcommand that preflights config, runs shared analysis/report generation, and sends no email."
    - "`tests/test_main_cli.py` covers the `report` command and proves it does not call the email path."
    - "`README.md` documents the supported no-email report workflow."
  key_links:
    - "`main.py report` uses the same Phase 1 bootstrap/preflight path as `run` and `test-email`, then calls the shared analysis/report modules."
    - "`main.py test-email` and `main.py run` both route through `analysis_core.py` and `report_pipeline.py` before any email or loop behavior."
    - "`predict_prophet.py` keeps `Prophet()` modeling and forecast plotting local, but imports shared loading/alignment helpers from `analysis_core.py`."
---

<objective>
Finish the shared-core refactor by routing supported commands through it and exposing a supported one-shot no-email report workflow for operators.

Purpose: Complete ANLY-02 and ANLY-03 by making the shared core the only supported path for operator-visible analysis/report generation.
Output: shared-core-driven CLI wiring, the `main.py report` command, forecast cleanup, CLI coverage, and updated operator docs.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-shared-analysis-core/02-CONTEXT.md
@.planning/phases/02-shared-analysis-core/02-PATTERNS.md
@analysis_core.py
@report_pipeline.py
@main.py
@predict_prophet.py
@README.md
@tests/test_main_cli.py

<interfaces>
Preserve the Phase 1 CLI surface:

```python
def main(argv=None):
    ...
```

Add this supported command behavior:

```python
python3 main.py report
```

The `report` subcommand must:
- run the same preflight/bootstrap path used by supported commands,
- generate report artifacts through `analysis_core.py` and `report_pipeline.py`,
- update configured HTML/chart outputs,
- exit non-zero on invalid config/data/dependencies,
- never call `send_email(...)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the supported no-email report command and CLI coverage</name>
  <files>main.py, tests/test_main_cli.py</files>
  <read_first>AGENTS.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/02-shared-analysis-core/02-CONTEXT.md, .planning/phases/02-shared-analysis-core/02-PATTERNS.md, analysis_core.py, report_pipeline.py, main.py, tests/test_main_cli.py</read_first>
  <behavior>
    - `main(["report"])` uses strict preflight for config, paths, and analysis dependencies, then runs the shared analysis/report core without invoking SMTP.
    - `main(["report"])` returns `1` when preflight fails or shared analysis/report generation raises a supported operator error.
    - `python3 main.py --help` shows `report` alongside `check`, `run`, `test-email`, and `show-config`.
  </behavior>
  <action>Extend `main.py` with a `report` subcommand on the same `argparse` surface as the Phase 1 commands. Wire `report` through `_load_command_context(...)`, `_check_analysis_dependencies()`, `load_asset_prices_reader(...)`, and the shared `analysis_core.py` / `report_pipeline.py` path so it generates the configured HTML and chart artifacts but never calls `send_email(...)` or `build_email_content(...)`. Use the exact subcommand name `report`. Update `tests/test_main_cli.py` to add coverage for `report` help text, preflight-gated failure behavior, and the guarantee that `report` does not invoke the email path when analysis succeeds. Keep `run`, `test-email`, and `show-config` on the same CLI surface without renaming them.</action>
  <verify>
    <automated>python3 -m unittest tests.test_main_cli -v &amp;&amp; python3 main.py --help | rg 'check|run|report|test-email|show-config'</automated>
  </verify>
  <acceptance_criteria>
    - `python3 main.py --help` includes `report`.
    - `tests/test_main_cli.py` contains coverage proving `report` does not call the SMTP path.
    - `python3 -m unittest tests.test_main_cli -v` exits `0`.
  </acceptance_criteria>
  <done>Operators can generate a no-email comparison report through the supported CLI, and the command is covered by the existing CLI test suite.</done>
</task>

<task type="auto">
  <name>Task 2: Remove stale supported call patterns and document the shared-core workflow</name>
  <files>main.py, predict_prophet.py, README.md</files>
  <read_first>AGENTS.md, .planning/ROADMAP.md, .planning/phases/02-shared-analysis-core/02-CONTEXT.md, .planning/phases/02-shared-analysis-core/02-PATTERNS.md, analysis_core.py, report_pipeline.py, main.py, predict_prophet.py, README.md</read_first>
  <action>Clean up stale supported-call patterns now that the shared core exists. In `main.py`, remove any leftover inline data-prep or report-assembly branches that bypass `analysis_core.py` or `report_pipeline.py`; the supported `run`, `test-email`, and `report` commands must all go through the shared modules. In `predict_prophet.py`, keep only Prophet-specific modeling and forecast-visualization logic local; any remaining duplicated supported prep helpers from Phase 2 scope should be replaced with imports from `analysis_core.py`. Update the focused README operator workflow so it documents `python3 main.py report` as the supported no-email report/charts path and makes clear that `run`, `test-email`, and `report` all share the same validated analysis core. Keep the README change scoped to the operator workflow and analysis/report commands; do not rewrite unrelated sections.</action>
  <verify>
    <automated>python3 -m py_compile main.py predict_prophet.py &amp;&amp; rg -n 'main\\.py report|no-email|shared analysis core' README.md &amp;&amp; rg -n 'from analysis_core import' predict_prophet.py</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` documents `python3 main.py report` as the supported no-email report path.
    - `predict_prophet.py` still contains `Prophet()` and forecast plotting, but imports shared prep helpers from `analysis_core.py`.
    - `python3 -m py_compile main.py predict_prophet.py` exits `0`.
  </acceptance_criteria>
  <done>The supported operator workflow now runs entirely through the shared analysis/report core, and the README reflects the new no-email report path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI `report` invocation -> shared runtime core | Operator-triggered no-email reports use the same bootstrap and shared analysis modules as alerting commands |
| shared report output -> operator workflow | Generated HTML/JPEG artifacts become a supported operator-facing outcome even without email |
| experimental Prophet path -> shared helpers | Experimental forecast code imports shared prep helpers but must not redefine supported command behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | D | `main.py report` | mitigate | Run the same strict preflight and dependency checks as other supported commands, and exit non-zero on any invalid analysis prerequisites |
| T-02-03-02 | I | no-email report workflow | mitigate | Ensure `report` never invokes SMTP or email-template paths; keep it artifact-only |
| T-02-03-03 | T | stale call paths | mitigate | Remove supported-code branches that bypass `analysis_core.py` or `report_pipeline.py` so behavior changes happen in one place only |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python3 -m unittest tests.test_main_cli -v`
- [ ] `python3 main.py --help | rg 'check|run|report|test-email|show-config'`
- [ ] `python3 -m py_compile main.py predict_prophet.py`
- [ ] `rg -n 'main\\.py report|no-email|shared analysis core' README.md`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- `main.py report` exists as the supported no-email report workflow
- Supported commands use the shared analysis/report core instead of stale inline paths
- Operators can generate the current comparison report from the refactored service code without patching scripts
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-analysis-core/02-03-SUMMARY.md`
</output>
