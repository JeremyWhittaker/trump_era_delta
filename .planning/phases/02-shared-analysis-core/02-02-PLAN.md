---
phase: 02-shared-analysis-core
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - report_pipeline.py
  - main.py
  - tests/test_report_pipeline.py
autonomous: true
requirements:
  - ANLY-02
must_haves:
  truths:
    - "Chart generation and statistical snapshot assembly for supported commands live behind one shared report contract instead of being rebuilt inline."
    - "The shared report contract preserves the current band semantics and chart artifact behavior."
    - "Supported runtime paths can obtain report artifacts and summary values from one function call."
  artifacts:
    - "`report_pipeline.py` exports the supported chart/report contract for comparison artifacts and statistical snapshot values."
    - "`tests/test_report_pipeline.py` verifies the shared contract keys, artifact-path behavior, and band snapshot fields."
    - "`main.py` consumes the shared report contract instead of rebuilding the `bands` dict and latest-stat snapshot inline."
  key_links:
    - "`report_pipeline.py` wraps the current `plot_comparison(...)` semantics and returns `zoomed_jpeg_path`, `full_jpeg_path`, `html_path`, `current_band`, `current_pct`, `regression_line`, `bands`, `latest_price`, and `latest_date`."
    - "`main.py::send_test_email_now(...)` and `main.py::main_loop(...)` both consume the same shared report result when preparing email payloads or no-email reports."
    - "Forecast-only Prophet visualization remains outside `report_pipeline.py`."
---

<objective>
Extract the supported chart/report-generation path into one reusable contract that returns artifacts and summary values without changing the meaning of the analysis.

Purpose: Deliver the report-generation half of ANLY-02 by moving the supported `main.py` comparison/report path behind one shared module while preserving current chart semantics.
Output: `report_pipeline.py`, direct tests for the report contract, and `main.py` using the shared snapshot/artifact result.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-shared-analysis-core/02-CONTEXT.md
@.planning/phases/02-shared-analysis-core/02-PATTERNS.md
@analysis_core.py
@main.py
@email_template.py

<interfaces>
Preserve the current supported artifact behavior:

```python
def plot_comparison(symbol, df_original, df_original_truncated, df_new, output_dir, sma_window, source, plot_bands=False, plot_bollinger_bands=False, original_start=None, original_end=None, new_start=None):
    ...
```

Create this shared supported-report interface:

```python
def build_band_snapshot(df_original_truncated, df_new):
    ...

def generate_comparison_report(symbol, source, df_original, df_original_truncated, df_new, output_dir, sma_window, plot_bands=False, plot_bollinger_bands=False, original_start=None, original_end=None, new_start=None):
    ...
```

`generate_comparison_report(...)` must return a dict with concrete keys:
`zoomed_jpeg_path`, `full_jpeg_path`, `html_path`, `current_band`, `current_pct`, `regression_line`, `bands`, `latest_price`, and `latest_date`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the shared supported-report contract</name>
  <files>report_pipeline.py, tests/test_report_pipeline.py</files>
  <read_first>AGENTS.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/02-shared-analysis-core/02-CONTEXT.md, .planning/phases/02-shared-analysis-core/02-PATTERNS.md, analysis_core.py, main.py, email_template.py</read_first>
  <behavior>
    - `build_band_snapshot(...)` returns the exact band dictionary and latest-stat values now assembled inline in `main.py`.
    - `generate_comparison_report(...)` wraps the current supported chart/report artifact generation and returns a stable dict keyed by artifact paths and summary values.
    - The shared report contract does not include SMTP sending or Prophet-only forecast rendering.
  </behavior>
  <action>Create `report_pipeline.py` as the shared supported-report module. Move the concrete `main.py::plot_comparison(...)` implementation into this module without changing chart semantics, band colors, hover content, or file-output behavior. Add `build_band_snapshot(df_original_truncated, df_new)` that constructs the exact `current_band`, `current_pct`, `regression_line`, `bands`, `latest_price`, and `latest_date` values currently rebuilt inline in `send_test_email_now(...)` and `main_loop(...)`. Add `generate_comparison_report(...)` that calls the shared plotting function, then returns a dict with the exact keys `zoomed_jpeg_path`, `full_jpeg_path`, `html_path`, `current_band`, `current_pct`, `regression_line`, `bands`, `latest_price`, and `latest_date`. Write `tests/test_report_pipeline.py` using stdlib `unittest` and `unittest.mock` to verify the returned dict keys, the `bands` mapping entries `+4σ` through `-4σ`, and that the plotting layer returns the expected artifact paths without invoking SMTP or CLI code.</action>
  <verify>
    <automated>python3 -m unittest tests.test_report_pipeline -v</automated>
  </verify>
  <acceptance_criteria>
    - `report_pipeline.py` contains `def build_band_snapshot(` and `def generate_comparison_report(`.
    - `python3 -m unittest tests.test_report_pipeline -v` exits `0`.
    - `tests/test_report_pipeline.py` proves the report result dict includes `zoomed_jpeg_path`, `full_jpeg_path`, `html_path`, `current_band`, `current_pct`, `regression_line`, `bands`, `latest_price`, and `latest_date`.
  </acceptance_criteria>
  <done>The supported chart/report path now has one reusable contract that callers can use without rebuilding statistics inline.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire supported runtime paths onto the shared report result</name>
  <files>main.py</files>
  <read_first>AGENTS.md, .planning/ROADMAP.md, .planning/phases/02-shared-analysis-core/02-CONTEXT.md, .planning/phases/02-shared-analysis-core/02-PATTERNS.md, analysis_core.py, report_pipeline.py, main.py, email_template.py</read_first>
  <action>Update `main.py` to import `generate_comparison_report(...)` from `report_pipeline.py` and remove the duplicated inline report-stat assembly currently present in `send_test_email_now(...)` and `main_loop(...)`. Replace the direct `plot_comparison(...)` calls plus ad hoc `bands` dictionary creation with the shared report result dict. Use the exact returned keys `zoomed_jpeg_path`, `full_jpeg_path`, `html_path`, `current_band`, `current_pct`, `regression_line`, `bands`, `latest_price`, and `latest_date` when building email content or copying the HTML output. Keep `build_email_content(...)` inputs semantically identical to current behavior; this plan must not redesign the email payload or band interpretation text. Remove the now-stale supported plotting function definition from `main.py` once the shared module is wired in.</action>
  <verify>
    <automated>python3 -m py_compile report_pipeline.py main.py &amp;&amp; rg -n 'from report_pipeline import' main.py &amp;&amp; ! rg -n '^def plot_comparison\\(' main.py</automated>
  </verify>
  <acceptance_criteria>
    - `main.py` imports `generate_comparison_report` from `report_pipeline.py`.
    - `main.py` no longer defines its own supported `plot_comparison(` function.
    - `python3 -m py_compile report_pipeline.py main.py` exits `0`.
  </acceptance_criteria>
  <done>The supported runtime paths now rely on one shared chart/report contract instead of reassembling output details inline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prepared analysis frames -> shared report contract | Shared data frames cross into chart rendering and summary-value assembly |
| shared report result -> email/report callers | Returned artifact paths and stats drive operator-visible output and alert payloads |
| shared report module -> filesystem output | The report layer writes HTML and JPEG artifacts to configured runtime paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | T | `report_pipeline.py` snapshot assembly | mitigate | Build the exact current `bands` keys and statistical fields from one helper so callers cannot drift on label names or missing values |
| T-02-02-02 | D | artifact generation | mitigate | Preserve the current explicit checks for missing chart artifacts and surface failures to callers rather than hiding them inside the report layer |
| T-02-02-03 | I | email/report payload inputs | mitigate | Keep `build_email_content(...)` inputs semantically identical while extracting the shared contract; do not redesign the payload in this phase |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python3 -m unittest tests.test_report_pipeline -v`
- [ ] `python3 -m py_compile report_pipeline.py main.py`
- [ ] `rg -n 'from report_pipeline import' main.py`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Supported chart/report generation lives in `report_pipeline.py`
- `main.py` no longer rebuilds report stats inline for each supported path
- Chart semantics and alert-facing statistical meaning remain unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-analysis-core/02-02-SUMMARY.md`
</output>
