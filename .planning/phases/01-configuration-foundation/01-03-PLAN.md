---
phase: 01-configuration-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - send_gmail.py
  - .env.local
  - send_test_email.py
  - email_recipients.txt
  - README.md
  - tests/test_send_gmail_config.py
autonomous: true
requirements:
  - CONF-02
  - CONF-03
must_haves:
  truths:
    - "Configured alert recipients come from the project-local config flow rather than the committed `email_recipients.txt` file."
    - "Gmail delivery code prefers the project-local `.env.local` contract while keeping the legacy home-directory fallback during migration."
    - "The legacy test-email entrypoint routes through the validated CLI instead of duplicating runtime logic."
  artifacts:
    - "`send_gmail.py` loads Gmail secrets through the shared Phase 1 secret contract."
    - "`.env.local` exists as the preferred gitignored secret file path."
    - "`send_test_email.py` is reduced to a thin wrapper over `main.py test-email`, and `README.md` points operators to the config-backed workflow."
  key_links:
    - "`send_gmail.py` delegates secret lookup to `service_config.load_gmail_secret_config(...)` while preserving the existing `(success, message)` send contract."
    - "`send_test_email.py` forwards directly into `main.main([...])` so test sends and live runs share the same bootstrap path."
    - "`README.md` and `email_recipients.txt` both point operators at `config/service.local.json -> alerts.recipients`."
---

<objective>
Finish the configuration migration for recipients and Gmail delivery so alert settings come from the new config flow everywhere the operator touches Phase 1.

Purpose: Complete D-03, D-04, and D-05 in the live mailer and legacy helper paths, while removing stale instructions that still point at committed recipient files.
Output: config-backed Gmail loader, project-local `.env.local` contract, config-backed test-email wrapper, and updated operator instructions.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-configuration-foundation/01-CONTEXT.md
@service_config.py
@service_bootstrap.py
@main.py
@send_gmail.py
@send_test_email.py
@email_recipients.txt
@README.md

<interfaces>
Use these shared contracts from earlier plans:

```python
def load_gmail_secret_config(env_file=None, env=None):
    ...

def main(argv=None):
    ...
```

Preserve this mailer contract:

```python
def load_config():
    ...

def send_email(to_addrs, subject, body, is_html=False, attachments=None,
               html_body=None, text_body=None, inline_images=None):
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route Gmail config through the shared Phase 1 secret contract</name>
  <files>send_gmail.py, .env.local, tests/test_send_gmail_config.py</files>
  <read_first>AGENTS.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-configuration-foundation/01-CONTEXT.md, service_config.py, send_gmail.py, .gitignore</read_first>
  <behavior>
    - `send_gmail.load_config()` prefers `.env.local` and only falls back to `~/.gmail_send/.env` when the project-local file is absent.
    - Missing or incomplete secret files return actionable error text that names `.env.local` first and the legacy fallback second.
    - The returned config still uses the existing keys `email` and `app_password` so `send_email(...)` keeps its public behavior.
  </behavior>
  <action>Update `send_gmail.py` so `load_config()` delegates to `service_config.load_gmail_secret_config(...)` instead of manually reading only `~/.gmail_send/.env`. Keep the public function name and the `(config, error)` return shape intact so existing callers continue to work. Update the module docstring and failure messages to describe `.env.local` as the preferred project-local secret path while still mentioning the legacy fallback per D-05. Create a gitignored `.env.local` placeholder only if it does not already exist; if the file is absent, write comment-safe starter lines for `GMAIL_ADDRESS=` and `GMAIL_APP_PASSWORD=` and do not fill real values. Add `tests/test_send_gmail_config.py` using stdlib `unittest` to prove `.env.local` wins over a temporary legacy file and that returned keys remain `email` / `app_password`.</action>
  <verify>
    <automated>python -m unittest tests.test_send_gmail_config -v &amp;&amp; rg -n '^GMAIL_ADDRESS=|^GMAIL_APP_PASSWORD=' .env.local</automated>
  </verify>
  <acceptance_criteria>
    - `send_gmail.py` mentions `.env.local` as the primary secret path and keeps the legacy fallback for migration.
    - `.env.local` exists after the task unless a real file already existed beforehand; the task must not overwrite an existing local secret file.
    - The automated test proves `.env.local` is preferred and that `load_config()` still returns `email` and `app_password` keys.
  </acceptance_criteria>
  <done>The live mailer now uses the shared config contract for Gmail secrets without breaking the existing SMTP send API.</done>
</task>

<task type="auto">
  <name>Task 2: Remove committed recipient dependence and collapse the legacy test-email helper into the unified CLI</name>
  <files>send_test_email.py, email_recipients.txt, README.md</files>
  <read_first>AGENTS.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-configuration-foundation/01-CONTEXT.md, main.py, send_test_email.py, email_recipients.txt, README.md</read_first>
  <action>Implement D-03 by removing `email_recipients.txt` as a runtime source of truth. Replace `send_test_email.py` with a thin wrapper that calls `main.main(["test-email", *sys.argv[1:]])` so the helper path shares the same validated bootstrap flow as `main.py test-email`. Do not leave duplicated analysis, plotting, or recipient-loading logic in the wrapper. Replace the contents of `email_recipients.txt` with a comment-only deprecation notice that points to `config/service.local.json -> alerts.recipients`; do not keep any real addresses in the repository. Update the targeted README setup steps that still tell the operator to create `email_recipients.txt` so they now instruct the operator to edit `config/service.local.json`, run `python main.py check`, and then use `python main.py test-email` or the thin wrapper. Keep the change focused to this config/CLI migration; do not rewrite unrelated README sections.</action>
  <verify>
    <automated>python send_test_email.py --help | rg 'test-email|--config|--local-config|--env-file' &amp;&amp; rg -n 'alerts\.recipients|main\.py check|main\.py test-email' README.md email_recipients.txt</automated>
  </verify>
  <acceptance_criteria>
    - `send_test_email.py` delegates to `main.main(...)` and no longer contains its own data-loading, plotting, or recipient-loading pipeline.
    - `email_recipients.txt` contains only a deprecation note or config pointer; it does not contain live recipient addresses.
    - `README.md` no longer instructs the operator to manage recipients through `email_recipients.txt`; it points to `config/service.local.json` and the `check` / `test-email` CLI flow instead.
  </acceptance_criteria>
  <done>Recipients and test-email operations are fully migrated to the new config-backed CLI flow, with no committed live-address dependency left in the repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env.local` / legacy env file -> SMTP auth | Local secret files provide Gmail credentials to the outbound mailer |
| project-local recipient config -> alert delivery | Configured recipient addresses determine who receives operational alerts |
| legacy helper -> unified CLI | The old wrapper script forwards user arguments into the validated Phase 1 CLI surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | I | `send_gmail.py` secret loading | mitigate | Prefer gitignored `.env.local`, preserve redacted messaging, and keep raw secrets out of logs and README examples |
| T-01-03-02 | I | `email_recipients.txt` and README setup instructions | mitigate | Remove live addresses from committed files and point operators to `config/service.local.json -> alerts.recipients` instead |
| T-01-03-03 | T | `send_test_email.py` wrapper | mitigate | Delegate directly to `main.main(...)` so the helper cannot drift away from the validated bootstrap path |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python -m unittest tests.test_send_gmail_config -v`
- [ ] `python send_test_email.py --help | rg 'test-email|--config|--local-config|--env-file'`
- [ ] `rg -n 'alerts\.recipients|main\.py check|main\.py test-email' README.md email_recipients.txt`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- `send_gmail.py` uses the project-local secret contract with legacy fallback
- Real recipients are no longer sourced from committed files
- The legacy test-email helper is only a thin entrypoint into the unified CLI
</success_criteria>

<output>
After completion, create `.planning/phases/01-configuration-foundation/01-03-SUMMARY.md`
</output>
