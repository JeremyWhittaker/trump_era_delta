---
phase: 03-alert-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alert_pipeline.py
  - main.py
  - tests/test_alert_pipeline.py
autonomous: true
requirements:
  - ALRT-01
  - ALRT-03
must_haves:
  truths:
    - "Test and live email paths build the same HTML/text payload and inline-image contract instead of composing separate email branches."
    - "The shared alert pipeline preserves the current email template semantics and SMTP transport contract."
    - "The only intentional test/live payload difference is explicit test labeling in the subject or payload metadata."
  artifacts:
    - "`alert_pipeline.py` exports the shared alert payload and delivery helpers for both test and live email sends."
    - "`tests/test_alert_pipeline.py` verifies subject labeling, HTML/text reuse, and inline-image assembly for the shared alert contract."
    - "`main.py` imports the shared alert helpers instead of keeping `_send_band_email(...)`, `_build_inline_images(...)`, and period-formatting logic inline."
  key_links:
    - "`main.py test-email` and the live alert path both call the same `alert_pipeline.py` helpers before SMTP delivery."
    - "`email_template.py::build_email_content(...)` remains the renderer and `send_gmail.py::send_email(...)` remains the transport boundary."
    - "Inline image CIDs stay stable across test and live emails so the same HTML body works in both modes."
---

<objective>
Create one shared alert email pipeline so test-email and live delivery use the same payload builder, inline-image handling, and SMTP contract.

Purpose: Deliver ALRT-01 and ALRT-03 by removing the remaining divergence between the supported test-email path and live alert sends.
Output: `alert_pipeline.py`, direct coverage for the alert payload contract, and `main.py` routing both email paths through it.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-alert-reliability/03-CONTEXT.md
@.planning/phases/03-alert-reliability/03-PATTERNS.md
@.planning/phases/02-shared-analysis-core/02-CONTEXT.md
@main.py
@report_pipeline.py
@email_template.py
@send_gmail.py

<interfaces>
Create this shared alert interface in `alert_pipeline.py`:

```python
def build_alert_payload(
    *,
    symbol,
    source,
    previous_band,
    report,
    days_original,
    days_new,
    sma_window,
    check_frequency,
    original_start,
    original_end,
    new_start,
    delivery_mode,
):
    ...

def send_alert_email(*, recipients, payload):
    ...
```

`build_alert_payload(...)` must return a dict with concrete keys:
`subject`, `html_body`, `text_body`, and `inline_images`.

`delivery_mode` must support at least `live` and `test`, with test mode adding explicit labeling while reusing the same rendered body and inline chart contract.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the shared alert payload and delivery contract</name>
  <files>alert_pipeline.py, tests/test_alert_pipeline.py</files>
  <read_first>AGENTS.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/03-alert-reliability/03-CONTEXT.md, .planning/phases/03-alert-reliability/03-PATTERNS.md, main.py, report_pipeline.py, email_template.py, send_gmail.py</read_first>
  <behavior>
    - `build_alert_payload(...)` reuses the current email template inputs, period labels, and report snapshot values without changing band meaning or interpretation text.
    - `build_alert_payload(...)` assembles the same inline image CIDs and bytes for both live and test paths, with only the subject/test label differing.
    - `send_alert_email(...)` preserves the existing `(success, message)` SMTP result contract from `send_gmail.py`.
  </behavior>
  <action>Create `alert_pipeline.py` as the shared alert helper module. Move the concrete logic now spread across `main.py::_format_period_context(...)`, `_build_inline_images(...)`, and `_send_band_email(...)` into this module. Implement `build_alert_payload(...)` so it calls `email_template.build_email_content(...)`, loads the generated chart bytes from the shared report result, and returns a dict with `subject`, `html_body`, `text_body`, and `inline_images`. In live mode, build the existing regression-band alert subject; in test mode, keep the same body and inline assets but prepend or otherwise clearly label the subject as a test email. Add `send_alert_email(...)` that accepts `recipients` plus a payload dict and forwards the normalized fields to `send_gmail.send_email(...)`. Write `tests/test_alert_pipeline.py` using stdlib `unittest` and temp files to prove test/live payloads share the same HTML/text bodies, inline image CIDs, and chart bytes, while differing only in explicit test labeling.</action>
  <verify>
    <automated>python3 -m unittest tests.test_alert_pipeline -v</automated>
  </verify>
  <acceptance_criteria>
    - `alert_pipeline.py` contains `def build_alert_payload(` and `def send_alert_email(`.
    - `python3 -m unittest tests.test_alert_pipeline -v` exits `0`.
    - `tests/test_alert_pipeline.py` proves the same rendered HTML/text and inline-image contract are used for both test and live payload construction.
  </acceptance_criteria>
  <done>The project has one shared alert payload and SMTP-delivery contract for both supported email entrypoints.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire `main.py` onto the shared alert pipeline</name>
  <files>main.py</files>
  <read_first>AGENTS.md, .planning/ROADMAP.md, .planning/phases/03-alert-reliability/03-CONTEXT.md, .planning/phases/03-alert-reliability/03-PATTERNS.md, alert_pipeline.py, main.py, report_pipeline.py</read_first>
  <action>Update `main.py` to import and use `build_alert_payload(...)` and `send_alert_email(...)` from `alert_pipeline.py` for both `send_test_email_now(...)` and the live alert branch in `main_loop(...)`. Remove the inline helpers `_format_period_context(...)`, `_build_inline_images(...)`, and `_send_band_email(...)` once the shared module is wired in. Keep the supported analysis/report path exactly as it is today: `main.py` still obtains a shared report dict from Phase 2, then hands that report into the shared alert pipeline. Do not reintroduce separate test-email composition logic in `send_test_email.py`; if that wrapper file is touched, keep it a thin forwarder to `main.py test-email` only.</action>
  <verify>
    <automated>python3 -m py_compile alert_pipeline.py main.py &amp;&amp; rg -n 'from alert_pipeline import' main.py &amp;&amp; ! rg -n '^def _send_band_email\\(|^def _build_inline_images\\(|^def _format_period_context\\(' main.py</automated>
  </verify>
  <acceptance_criteria>
    - `main.py` imports from `alert_pipeline.py`.
    - `main.py` no longer defines `_send_band_email(`, `_build_inline_images(`, or `_format_period_context(`.
    - `python3 -m py_compile alert_pipeline.py main.py` exits `0`.
  </acceptance_criteria>
  <done>The supported email paths now share one alert composition and delivery contract instead of carrying duplicate inline-email logic in the controller.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared report result -> alert payload builder | The report snapshot and chart artifacts become operator-visible email content |
| alert payload builder -> SMTP transport | Normalized HTML/text/inline-image parts cross into Gmail transport |
| test/live command paths -> shared alert contract | Both supported commands now rely on one payload builder, so any regression affects both |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | T | `alert_pipeline.py` payload composition | mitigate | Keep `build_email_content(...)` authoritative for rendered body semantics and prove shared test/live reuse with direct unit tests |
| T-03-01-02 | I | inline image contract | mitigate | Preserve the current `chart_zoomed` / `chart_full` CID expectations and verify that both modes build the same inline-image list |
| T-03-01-03 | D | SMTP wrapper | mitigate | Keep the existing `(success, message)` contract from `send_gmail.py` so callers can handle failures without changing transport semantics |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python3 -m unittest tests.test_alert_pipeline -v`
- [ ] `python3 -m py_compile alert_pipeline.py main.py`
- [ ] `rg -n 'from alert_pipeline import' main.py`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Test-email and live alert delivery use one shared payload and inline-image contract
- `main.py` no longer rebuilds alert email parts inline
- The rendered email semantics remain compatible with the current template and SMTP transport
</success_criteria>

<output>
After completion, create `.planning/phases/03-alert-reliability/03-01-SUMMARY.md`
</output>
