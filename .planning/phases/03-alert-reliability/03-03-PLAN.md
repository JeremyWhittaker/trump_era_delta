---
phase: 03-alert-reliability
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - main.py
  - send_test_email.py
  - README.md
  - tests/test_main_cli.py
autonomous: true
requirements:
  - ALRT-01
  - ALRT-02
must_haves:
  truths:
    - "Operators have explicit supported verification entrypoints for both the test-email path and a one-cycle live alert evaluation."
    - "The live verification path reuses the same persisted-state and alert-delivery contracts as the long-running monitor instead of inventing a separate simulation path."
    - "The legacy helper script remains a thin forwarder rather than a second alert implementation."
  artifacts:
    - "`main.py run --once` executes one validated live-monitor cycle and exits without sleeping."
    - "`tests/test_main_cli.py` covers `test-email`, `run --once`, and the guarantee that one-shot live verification uses the shared alert pipeline."
    - "`README.md` documents the focused operator verification workflow for `check`, `test-email`, and `run --once`."
  key_links:
    - "`main.py test-email` remains the real SMTP acceptance path and `main.py run --once` becomes the explicit live-alert verification path."
    - "`run --once` uses the same analysis core, alert pipeline, and persisted state helpers as the long-running `run` command."
    - "`send_test_email.py` continues forwarding directly to `main.py test-email` only."
---

<objective>
Expose explicit operator verification commands for both test and live alerting over the shared pipeline, without requiring the full long-running service loop.

Purpose: Close the Phase 3 operator gap by making email verification and one-cycle live alert evaluation directly runnable and testable from the supported CLI.
Output: `run --once`, CLI coverage for the shared verification flows, and focused operator docs for the Phase 3 alert workflow.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-alert-reliability/03-CONTEXT.md
@.planning/phases/03-alert-reliability/03-PATTERNS.md
@.planning/phases/03-alert-reliability/03-01-PLAN.md
@.planning/phases/03-alert-reliability/03-02-PLAN.md
@main.py
@send_test_email.py
@README.md
@tests/test_main_cli.py

<interfaces>
Preserve the supported CLI surface in `main.py` and add this verification behavior:

```bash
python3 main.py test-email
python3 main.py run --once
```

`run --once` must:
- run the same preflight/bootstrap path as `run`,
- execute exactly one live analysis + alert evaluation cycle,
- use persisted alert state and the shared alert pipeline,
- exit without sleeping or entering the long-running loop,
- return non-zero on supported analysis or delivery failure.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add one-cycle live verification to the supported CLI</name>
  <files>main.py, tests/test_main_cli.py</files>
  <read_first>AGENTS.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/03-alert-reliability/03-CONTEXT.md, .planning/phases/03-alert-reliability/03-PATTERNS.md, alert_pipeline.py, alert_state.py, main.py, tests/test_main_cli.py</read_first>
  <behavior>
    - `main(["run", "--once"])` executes a single live-monitor cycle with the same preflight, analysis, persisted-state, and alert-delivery helpers used by the long-running service.
    - `main(["run", "--once"])` never sleeps and returns `1` on fatal analysis or delivery failure.
    - `main(["test-email"])` remains the real SMTP acceptance path and continues using the shared alert pipeline from Wave 1.
  </behavior>
  <action>Extend the `run` subcommand in `main.py` with a `--once` flag that executes one supported live-monitor cycle and exits. Refactor the current loop body into a callable helper if needed so both the long-running `run` path and `run --once` share the same analysis, alert-state, and alert-delivery logic. Update `tests/test_main_cli.py` to cover `run --once` preflight behavior, the no-sleep guarantee for one-shot mode, and the expectation that `run --once` uses the same shared alert path instead of bypassing state or SMTP helpers. Keep `test-email` on the same CLI surface and ensure its tests still prove it does not run when preflight fails.</action>
  <verify>
    <automated>python3 -m unittest tests.test_main_cli -v &amp;&amp; python3 main.py --help | rg 'test-email|run' </automated>
  </verify>
  <acceptance_criteria>
    - `main.py` accepts `run --once`.
    - `tests/test_main_cli.py` proves `run --once` does not sleep and routes through the shared live alert evaluation path.
    - `python3 -m unittest tests.test_main_cli -v` exits `0`.
  </acceptance_criteria>
  <done>Operators can verify one live alert cycle from the supported CLI without starting the full background monitor.</done>
</task>

<task type="auto">
  <name>Task 2: Keep helper surfaces thin and document the Phase 3 verification workflow</name>
  <files>send_test_email.py, README.md</files>
  <read_first>AGENTS.md, .planning/ROADMAP.md, .planning/phases/03-alert-reliability/03-CONTEXT.md, .planning/phases/03-alert-reliability/03-PATTERNS.md, main.py, send_test_email.py, README.md</read_first>
  <action>Verify that `send_test_email.py` remains a thin wrapper over `main.py test-email`; if any stale logic or comments contradict the shared Phase 3 path, remove that drift instead of rebuilding functionality there. Update the focused operator workflow in `README.md` so it documents `python3 main.py test-email` as the real email acceptance check and `python3 main.py run --once` as the explicit live-alert verification path before enabling the long-running service. Keep the README edits scoped to the verification workflow; full service-manager install docs remain Phase 4 and full handoff material remains Phase 5.</action>
  <verify>
    <automated>python3 -m py_compile main.py send_test_email.py &amp;&amp; rg -n 'test-email|run --once' README.md send_test_email.py</automated>
  </verify>
  <acceptance_criteria>
    - `send_test_email.py` remains a direct wrapper around `main.py test-email`.
    - `README.md` documents `python3 main.py test-email` and `python3 main.py run --once` as the focused Phase 3 verification workflow.
    - `python3 -m py_compile main.py send_test_email.py` exits `0`.
  </acceptance_criteria>
  <done>The project exposes a clear, supported verification workflow for both test and live alerting without reviving stale helper scripts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI -> one-cycle live evaluator | A direct operator command can now trigger stateful live alert logic without the long-running loop |
| one-cycle live evaluator -> persisted alert state | Verification mode still updates real on-disk alert state and must remain faithful to production behavior |
| legacy wrapper -> supported CLI | `send_test_email.py` must forward into the supported CLI rather than diverging again |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | T | `run --once` divergence | mitigate | Reuse the same cycle helper as the long-running monitor so one-shot verification cannot drift from live behavior |
| T-03-03-02 | D | one-shot verification | mitigate | Exit without sleeping and return non-zero on supported failures so operators can trust the command outcome |
| T-03-03-03 | E | legacy wrapper drift | mitigate | Keep `send_test_email.py` as a direct forwarder and document the supported verification commands in one place |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python3 -m unittest tests.test_main_cli -v`
- [ ] `python3 main.py --help | rg 'test-email|run'`
- [ ] `python3 -m py_compile main.py send_test_email.py`
- [ ] `rg -n 'test-email|run --once' README.md send_test_email.py`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Operators can run explicit supported verification commands for both test-email and one-cycle live alert evaluation
- `run --once` uses the same persisted-state and delivery helpers as the long-running monitor
- `send_test_email.py` stays a thin forwarder instead of becoming a second implementation path
</success_criteria>

<output>
After completion, create `.planning/phases/03-alert-reliability/03-03-SUMMARY.md`
</output>
