---
phase: 03-alert-reliability
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - alert_state.py
  - main.py
  - tests/test_alert_state.py
autonomous: true
requirements:
  - ALRT-02
  - ALRT-04
must_haves:
  truths:
    - "Live alert evaluation persists enough local state to survive restarts without replaying already delivered transitions."
    - "A failed alert send leaves retryable pending state on disk instead of incorrectly advancing the delivered marker."
    - "The live monitor no longer relies on process-local `previous_band` memory alone."
  artifacts:
    - "`alert_state.py` owns alert-state path resolution, JSON load/save, and transition bookkeeping helpers."
    - "`tests/test_alert_state.py` covers fresh state, atomic save/load round-trips, pending retry semantics, and delivered-transition idempotency."
    - "`main.py` loads persisted alert state before evaluation and updates it only after observation and delivery outcomes are known."
  key_links:
    - "Persisted state includes `last_observed_band`, `pending_transition`, and `last_delivered_transition` or equivalent fields that distinguish observed from delivered state."
    - "The live alert branch in `main.py` updates state after every valid analysis cycle, not just after successful sends."
    - "State writes happen through `alert_state.py`, not ad hoc JSON handling inside `main.py`."
---

<objective>
Persist live alert state and apply restart-safe transition logic so the monitor can retry failed sends and avoid duplicate delivered alerts after restart.

Purpose: Deliver ALRT-02 and ALRT-04 by replacing process-local band tracking with a small persisted state machine.
Output: `alert_state.py`, direct tests for state semantics, and `main.py` live delivery wired to persisted transition state.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-alert-reliability/03-CONTEXT.md
@.planning/phases/03-alert-reliability/03-PATTERNS.md
@.planning/phases/03-alert-reliability/03-01-PLAN.md
@main.py
@service_config.py
@alert_pipeline.py

<interfaces>
Create this persisted-state interface in `alert_state.py`:

```python
def resolve_alert_state_path(log_path=None, configured_path=None):
    ...

def load_alert_state(path):
    ...

def save_alert_state(path, state):
    ...

def evaluate_transition(state, current_band, observed_at):
    ...

def record_delivery_result(state, transition, delivered, error_message=None):
    ...
```

The persisted state must distinguish:
- last observed band,
- a pending undelivered transition,
- the last successfully delivered transition.

`save_alert_state(...)` must use an atomic-write pattern rather than rewriting the target file in place.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the persisted alert-state helper and its direct tests</name>
  <files>alert_state.py, tests/test_alert_state.py</files>
  <read_first>AGENTS.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/03-alert-reliability/03-CONTEXT.md, .planning/phases/03-alert-reliability/03-PATTERNS.md, main.py, service_config.py</read_first>
  <behavior>
    - `load_alert_state(...)` returns a safe default state when the file is absent and raises an actionable error for malformed persisted state.
    - `evaluate_transition(...)` can distinguish first observation, no-change, new transition, and retryable pending transition without depending on process-local globals.
    - `record_delivery_result(...)` advances the delivered marker only on confirmed SMTP success and preserves pending state on failure.
  </behavior>
  <action>Create `alert_state.py` as the root-level alert-state module. Implement `resolve_alert_state_path(...)` so the monitor uses a project-local runtime path by default, with an optional explicit override when one is supplied. Implement `load_alert_state(...)` and `save_alert_state(...)` around a lightweight JSON schema that stores at least `last_observed_band`, `pending_transition`, and `last_delivered_transition`, and write the file atomically via a temp file plus rename/replace. Implement `evaluate_transition(...)` to return a concrete transition decision for first observation, unchanged band, new band change, or retry of an existing undelivered transition. Implement `record_delivery_result(...)` so successful sends clear pending state and advance the delivered marker, while failed sends keep a retryable pending transition and optionally annotate the last error. Add `tests/test_alert_state.py` with stdlib `unittest` and temp files to prove default-state loading, malformed JSON failure, atomic save/load round-trip behavior, no-resend semantics for already delivered transitions, and retry behavior for failed deliveries after restart.</action>
  <verify>
    <automated>python3 -m unittest tests.test_alert_state -v</automated>
  </verify>
  <acceptance_criteria>
    - `alert_state.py` contains `def resolve_alert_state_path(`, `def load_alert_state(`, `def save_alert_state(`, `def evaluate_transition(`, and `def record_delivery_result(`.
    - `python3 -m unittest tests.test_alert_state -v` exits `0`.
    - `tests/test_alert_state.py` proves failed delivery keeps pending state and delivered transitions are not re-emitted on the next load.
  </acceptance_criteria>
  <done>The project has a tested persisted alert-state helper that can survive restart and distinguish observed vs delivered transitions.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire the live alert loop onto persisted state</name>
  <files>main.py</files>
  <read_first>AGENTS.md, .planning/ROADMAP.md, .planning/phases/03-alert-reliability/03-CONTEXT.md, .planning/phases/03-alert-reliability/03-PATTERNS.md, alert_pipeline.py, alert_state.py, main.py</read_first>
  <action>Update `main.py` so the supported live alert path loads persisted alert state before evaluating transitions and writes updated state after each valid analysis cycle. Replace the current process-local `previous_band` branch in `main_loop(...)` with helper-driven transition evaluation from `alert_state.py`. When a transition is new or retryable, build the shared alert payload from `alert_pipeline.py`, attempt delivery, and call `record_delivery_result(...)` with the outcome before saving state. Keep the current hard-fail behavior for fatal analysis errors, but do not crash the process solely because SMTP delivery failed; instead log the failure, persist the pending transition, and continue to the next cycle. Preserve existing chart/band semantics from Phase 2 and do not redesign the report snapshot or email content in this plan.</action>
  <verify>
    <automated>python3 -m py_compile alert_state.py main.py &amp;&amp; ! rg -n 'previous_band = None|elif current_band != previous_band' main.py</automated>
  </verify>
  <acceptance_criteria>
    - `main.py` uses `alert_state.py` for live transition tracking instead of process-local `previous_band`-only logic.
    - SMTP failures in the live alert path leave persisted pending state rather than marking the transition delivered.
    - `python3 -m py_compile alert_state.py main.py` exits `0`.
  </acceptance_criteria>
  <done>The live monitor loop now uses restart-safe persisted state for alert transition tracking and retry decisions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem runtime state -> live alert evaluator | Persisted JSON now influences whether a transition is sent, retried, or skipped |
| live analysis result -> state machine | Current band observations become durable alert-state updates |
| SMTP result -> persisted delivery markers | Delivery outcome determines whether state advances or remains pending |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | T | persisted alert JSON | mitigate | Validate and normalize loaded state through one helper and fail with actionable errors on malformed files |
| T-03-02-02 | D | delivery-marker bookkeeping | mitigate | Advance the delivered marker only after confirmed SMTP success; retain pending state on failures |
| T-03-02-03 | R | restart behavior | mitigate | Persist both observed and delivered transition markers so the monitor can explain and reproduce why a transition was retried or skipped |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python3 -m unittest tests.test_alert_state -v`
- [ ] `python3 -m py_compile alert_state.py main.py`
- [ ] `! rg -n 'previous_band = None|elif current_band != previous_band' main.py`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Live alert evaluation persists restart-safe state in project-local runtime storage
- Failed sends remain pending for retry instead of corrupting delivered-state bookkeeping
- Already delivered transitions are not re-emitted just because the process restarts
</success_criteria>

<output>
After completion, create `.planning/phases/03-alert-reliability/03-02-SUMMARY.md`
</output>
